# 🔧 Исправление URL для второго сервера

## Проблема

Для второго сервера (`147.45.50.129:45087`) в базе данных указан неправильный порт.

## ❌ Неправильно

```
http://127.0.0.1:45087/.../panel/api
```

Порт `45087` - это порт 3x-UI на VPN сервере, но туннель на хосте слушает на порту `38869`.

## ✅ Правильно

```
http://127.0.0.1:38869/.../panel/api
```

Порт `38869` - это порт туннеля на хосте, через который контейнер подключается.

## 🔍 Как исправить

### Вариант 1: Через админ-панель

1. Зайдите в админ-панель → Сервера
2. Найдите сервер "FioreVPN Server 1 Netherlands" (или другой, который использует `147.45.50.129`)
3. В поле "3x-UI API URL" измените:
   - **Было**: `http://127.0.0.1:45087/.../panel/api`
   - **Стало**: `http://127.0.0.1:38869/.../panel/api`
4. Сохраните

### Вариант 2: Через SQL

```sql
-- Найти сервер с неправильным портом
SELECT id, name, x3ui_api_url 
FROM servers 
WHERE x3ui_api_url LIKE '%45087%';

-- Исправить порт
UPDATE servers 
SET x3ui_api_url = REPLACE(x3ui_api_url, ':45087', ':38869')
WHERE x3ui_api_url LIKE '%45087%';
```

## 📋 Схема портов

**Первый сервер (62.133.60.47):**
- VPN сервер: `127.0.0.1:38868` (3x-UI)
- Туннель на хосте: `0.0.0.0:38868` → `127.0.0.1:38868`
- URL в БД: `http://127.0.0.1:38868/.../panel/api` ✅

**Второй сервер (147.45.50.129):**
- VPN сервер: `127.0.0.1:45087` (3x-UI)
- Туннель на хосте: `0.0.0.0:38869` → `127.0.0.1:45087`
- URL в БД: `http://127.0.0.1:38869/.../panel/api` ✅ (не 45087!)

## ✅ Проверка

После исправления проверьте:

```bash
# Проверьте, что туннель работает
ss -tulpn | grep 38869
# Должно быть: 0.0.0.0:38869

# Проверьте из контейнера
docker compose -f docker-compose.prod.yml exec core curl -s -o /dev/null -w "%{http_code}" http://host.docker.internal:38869
# Должен вернуть HTTP код (200, 401, 403 и т.д.), не ошибку подключения
```

